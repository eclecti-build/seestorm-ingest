app = "seestorm-ingest-mountain"
primary_region = "dfw"  # Dallas — closest Fly region to Rockies/Mountain West

[build]

[env]
  POLL_INTERVAL = "30s"
  SNAPSHOT_DIR = "/data/snapshots"

  NWS_AREA = "CO,WY,MT,ND,NM,AZ,UT,NV,ID"

[mounts]
  source = "seestorm_data"
  destination = "/data"
