app = "seestorm-ingest-dixie"
primary_region = "iad"  # Ashburn — closest Fly region to Dixie Alley

[build]

[env]
  POLL_INTERVAL = "30s"
  SNAPSHOT_DIR = "/data/snapshots"

  NWS_AREA = "AL,MS,TN,AR,GA"

[mounts]
  source = "seestorm_data"
  destination = "/data"
